{% extends '::base.html.twig' %}

{% set active_section = 'goods' %}

{% block body -%}
    <div class="page-header">
        <h1>Edit Good</h1>
    </div>

    {# TASK 1: render form #}
{#{{ form_start(edit_form) }}
    {{ form_rest(edit_form) }}
    {{ form_end(edit_form) }}#}

    {#TASK 4#}
    {% include 'DeepSpaceOneGameBundle:Good:form.html.twig' with {'form' : edit_form} %}


    <ul class="record_actions list-inline">
        <li><a href="{{ path('goods') }}" class="btn btn-default"><span class="glyphicon glyphicon-list"></span> Back to the list</a></li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
